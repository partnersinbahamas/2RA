import { default as React } from 'react';

interface IProps {
    className?: string;
    color?: string;
    style?: React.CSSProperties;
    withBox?: boolean;
}
declare const SheetProgressIcon: React.FC<IProps>;
export default SheetProgressIcon;
