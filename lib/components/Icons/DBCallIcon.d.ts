import { default as React, CSSProperties } from 'react';

type TProps = {
    className?: string;
    color?: string;
    style?: CSSProperties;
    withBox?: boolean;
};
declare const DBCallIcon: React.FC<TProps>;
export default DBCallIcon;
